/** @file Shared theme state; retains the existing admin import API. */

export type { ThemePreference } from '@supernova/design-system/theme'
export {
  getStoredThemePreference,
  resolveTheme,
  themeOptions,
  useThemeStore,
} from '@supernova/design-system/theme'

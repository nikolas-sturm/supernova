# Eclipse Core Extension

Neutralino starts the native C++ extension from `bin/`. CMake writes platform binaries here so
`neu run` and `neu build` use the same artifact.

Do not commit files under `bin/`.
